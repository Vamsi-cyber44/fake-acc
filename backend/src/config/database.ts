export async function connectDB() {
  // Minimal stub: replace with real MongoDB connection logic when restoring
  return Promise.resolve();
}

export async function disconnectDB() {
  return Promise.resolve();
}
